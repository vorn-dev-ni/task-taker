import { getNote, postNote,getSingleNote,deleteNote,updateNote  } from "./todoNoteController";
export const todoNoteController = {
  getNote,
  postNote,
  getSingleNote,
  deleteNote,
  updateNote
};
